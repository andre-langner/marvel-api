spring:
  application:
    name: Marvel API
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:h2:mem:marvel;DB_CLOSE_DELAY=-1
    username:
    password:
  jpa:
    show-sql: false
